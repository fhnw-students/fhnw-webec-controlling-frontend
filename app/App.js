/**
 * @name JIRA Worklog Controlling
 * @description
 *
 *
 * @version 0.0.0
 */
require(['Router', 'jquery'], function (Router, $) {
  /**
   * Activates the Router to listen to the hashchanges
   */
  $(document).ready(function () {
    Router.start();
  });
});
